if global.player_score >= 50 && global.spawn_permission_right_hand = false {
	global.spawn_permission_right_hand = true	
	}	
	
	if global.player_score >= 200 && global.spawn_permission_left_hand = false {
	global.spawn_permission_left_hand = true
	
	}
	if global.player_score >= 500 && global.spawn_permission_boulders = false {
	global.spawn_permission_boulders = true
	
	}
	if global.player_score >= 1000 && global.spawn_permission_sythes = false {
	global.spawn_permission_sythes = true
	
	}
	if global.player_score >= 1000 && global.spawn_permission_pumpkin = false {
	global.spawn_permission_pumpkin = true
	
	}
	if global.player_score >= 1500 && global.spawn_permission_records = false {
	global.spawn_permission_records = true
	
	}
	if global.player_score >= 2000 && global.spawn_permission_death = false{
	global.spawn_permission_death = true	
	
	
	}
	